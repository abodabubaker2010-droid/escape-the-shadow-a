## Packages
framer-motion | Page transitions and UI animations
lucide-react | Icons for UI

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Architects Daughter'", "cursive"],
  body: ["'DM Sans'", "sans-serif"],
  mono: ["'Fira Code'", "monospace"],
}

The game engine will be implemented using HTML5 Canvas within a React component.
Levels are fetched from the API and contain JSON configuration for platforms, spawns, etc.
